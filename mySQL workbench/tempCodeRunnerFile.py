#to pass the data execution using cursor
# mycursor = myconnection.cursor()

# #create table in my sql
# firstTable = "create table if not exists sports(primary text, secondary text)"
# mycursor.execute(firstTable)
# mycursor.commit()